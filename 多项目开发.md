# ROS2 多项目开发指南

本文档介绍如何在同一工作区中开发多个ROS2项目（Python版本和C++版本）。

## 项目结构

```
ROS/
├── ros2_ws/                    # Python版本ROS2项目
│   └── src/example_pkg/
│       ├── talker.py
│       └── listener.py
├── ros2_cp/                    # C++版本ROS2项目
│   └── src/example_pkg_cp/
│       ├── talker.cpp
│       └── listener.cpp
├── ros2_ws.sh                  # Python项目Docker脚本
├── ros2_cp.sh                  # C++项目Docker脚本
└── .devcontainer/
    ├── devcontainer.json       # 默认配置 (Python项目)
    ├── ros2_ws/
    │   └── devcontainer.json   # Python项目专用容器
    └── ros2_cp/
        └── devcontainer.json   # C++项目专用容器
```

---

## 方案1：单容器多项目

修改 `.devcontainer/devcontainer.json`，同时挂载两个项目：

```json
{
    "name": "ROS 2 Humble Development",
    "image": "osrf/ros:humble-desktop-full",
    "mounts": [
        "source=/tmp/.X11-unix,target=/tmp/.X11-unix,type=bind,consistency=cached",
        "source=${localWorkspaceFolder}/ros2_ws,target=/root/ros2_ws,type=bind,consistency=cached",
        "source=${localWorkspaceFolder}/ros2_cp,target=/root/ros2_cp,type=bind,consistency=cached"
    ],
    "workspaceFolder": "/root/ros2_ws",
    ...
}
```

容器内目录结构：
- `/root/ros2_ws` - Python版本项目
- `/root/ros2_cp` - C++版本项目

### 使用方法

进入容器后，通过命令切换工作目录：

```bash
# Python项目
cd /root/ros2_ws
colcon build
source install/setup.bash

# C++项目
cd /root/ros2_cp
colcon build
source install/setup.bash
```

---

## 方案2：独立容器配置 ✅ 已实现

创建多个devcontainer配置文件，实现启动时选择：

```
.devcontainer/
├── devcontainer.json          # 默认配置 (Python项目)
├── ros2_ws/
│   └── devcontainer.json      # Python项目专用容器
└── ros2_cp/
    └── devcontainer.json      # C++项目专用容器
```

### 配置说明

**ros2_ws/devcontainer.json (Python项目):**
- 容器名称: `ROS 2 Python (ros2_ws)`
- 工作目录: `/root/ros2_ws`
- 扩展: Python, Debugpy, ROS

**ros2_cp/devcontainer.json (C++项目):**
- 容器名称: `ROS 2 C++ (ros2_cp)`
- 工作目录: `/root/ros2_cp`
- 扩展: C/C++, C++ Extension Pack, ROS

### 使用方法

1. 按 `Ctrl+Shift+P`
2. 选择 `Dev Containers: Reopen in Container`
3. VS Code 会弹出配置选择菜单：
   - `ROS 2 Humble Development` - 默认(Python项目)
   - `ROS 2 Python (ros2_ws)` - Python项目专用
   - `ROS 2 C++ (ros2_cp)` - C++项目专用
4. 选择需要进入的项目容器

---

## Docker脚本使用

### Python项目 (ros2_ws.sh)

```bash
# 启动容器
./ros2_ws.sh start

# 编译项目
./ros2_ws.sh build

# 运行发布者
./ros2_ws.sh talker

# 运行订阅者
./ros2_ws.sh listener

# 进入容器
./ros2_ws.sh shell

# 停止容器
./ros2_ws.sh stop
```

### C++项目 (ros2_cp.sh)

```bash
# 启动容器
./ros2_cp.sh start

# 编译项目
./ros2_cp.sh build

# 运行发布者
./ros2_cp.sh talker

# 运行订阅者
./ros2_cp.sh listener

# 进入容器
./ros2_cp.sh shell

# 停止容器
./ros2_cp.sh stop
```

---

## 方案对比

| 特性 | 方案1（单容器多项目） | 方案2（独立容器）✅ |
|------|---------------------|------------------|
| 资源占用 | 低（一个容器） | 高（多个容器） |
| 切换项目 | 手动cd切换 | 启动时选择 |
| 项目隔离 | 低 | 高 |
| 开发环境 | 共享Python扩展 | 分别配置扩展 |
| 适用场景 | 同时开发多个项目 | 项目独立开发 |

---

## 注意事项

1. **ROS2环境变量**：每个项目编译后需要 `source install/setup.bash`
2. **端口冲突**：如果使用相同端口，不同容器的节点可能冲突
3. **X11转发**：确保运行 `xhost +local:docker` 以支持GUI应用
4. **容器独立**：方案2的各项目容器完全独立，不会互相干扰
