# VSCode Dev Container 开发指南

## 一、开发模式原理

### 1.1 架构图解

```
┌─────────────────────────────────────────────────────────────┐
│                        宿主机                                │
│                                                             │
│  ┌─────────────────┐          ┌─────────────────────────┐  │
│  │   VSCode 客户端  │◄────────►│   本地源代码目录         │  │
│  │   (UI 界面)      │          │   /media/wht/N/ROS/...  │  │
│  └────────┬────────┘          └──────────┬──────────────┘  │
│           │                              │ 挂载            │
│           │ VSCode Server                ▼                 │
│           │                    ┌─────────────────────────┐ │
│           └───────────────────►│   Docker 容器            │ │
│                                │   (ros:humble-desktop)  │ │
│                                │   - ROS2 环境           │ │
│                                │   - 编译工具链          │ │
│                                │   - 运行时依赖          │ │
│                                └─────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 工作原理

| 组件 | 作用 |
|------|------|
| **VSCode 客户端** | 运行在宿主机，负责 UI 显示 |
| **VSCode Server** | 运行在容器内，负责代码处理、终端、调试 |
| **文件挂载** | 宿主机代码目录 ↔ 容器内工作目录双向同步 |
| **端口转发** | 容器内服务端口自动映射到宿主机 |

### 1.3 开发方式对比

| 方式 | 优点 | 缺点 |
|------|------|------|
| **Dev Container** | 配置即代码、环境一致、团队共享 | 每次新建容器、需要配置文件 |
| **Attach to Container** | 复用已有容器、灵活 | 手动管理容器、环境可能不一致 |

---

## 二、使用方法

### 2.1 进入容器开发

```
Ctrl+Shift+P → Dev Containers: Reopen in Container
```

### 2.2 典型开发流程

```bash
# 容器内终端操作
cd /workspaces/ros2_ws  # 或你的工作目录

# 创建 ROS2 包
ros2 pkg create --build-type ament_cmake my_package

# 编译
colcon build

# 运行节点
ros2 run my_package my_node
```

### 2.3 调试配置

在 `.vscode/launch.json` 中配置：

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "ROS2 Debug",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/install/my_package/lib/my_package/my_node",
      "cwd": "${workspaceFolder}"
    }
  ]
}
```

### 2.4 常用快捷操作

| 操作 | 快捷键/命令 |
|------|-------------|
| 进入容器 | `Ctrl+Shift+P` → `Reopen in Container` |
| 回到本地 | `Ctrl+Shift+P` → `Reopen Folder Locally` |
| 重建容器 | `Ctrl+Shift+P` → `Rebuild Container` |
| 查看容器日志 | `Ctrl+Shift+P` → `Show Container Log` |

---

## 三、配置文件详解

### 3.1 devcontainer.json 基础配置

创建 `.devcontainer/devcontainer.json`：

```json
{
  "name": "ROS2 Humble Dev",
  "image": "osrf/ros:humble-desktop-full",
  "workspaceFolder": "/workspaces/ros2_ws",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/ros2_ws,type=bind",
  "extensions": [
    "ms-iot.vscode-ros"
  ],
  "postCreateCommand": "apt-get update && apt-get install -y gdb"
}
```

### 3.2 使用 Docker Compose 方式

创建 `.devcontainer/docker-compose.yml`：

```yaml
version: '3.8'
services:
  ros2-humble:
    image: osrf/ros:humble-desktop-full
    container_name: ros2-humble
    volumes:
      - ..:/workspaces/ros2_ws
    command: sleep infinity
```

`devcontainer.json` 引用它：

```json
{
  "name": "ROS2 Humble",
  "dockerComposeFile": "docker-compose.yml",
  "service": "ros2-humble",
  "workspaceFolder": "/workspaces/ros2_ws"
}
```

---

## 四、容器持久性

### 4.1 容器生命周期

```
┌─────────────────────────────────────────────────────────┐
│                    容器生命周期                          │
│                                                         │
│   创建 ──► 运行 ──► 停止 ──► 重启 ──► 停止 ──► 删除     │
│   (首次)      (开发中)  (退出VSCode)  (再次打开)         │
└─────────────────────────────────────────────────────────┘
```

### 4.2 默认行为

| 场景 | 行为 |
|------|------|
| 关闭 VSCode | 容器**停止**，但**不会自动删除** |
| 再次 Reopen in Container | **重启**原有容器（不是新建） |
| 手动 Rebuild Container | 删除旧容器，**新建**容器 |

### 4.3 数据持久化情况

| 数据类型 | 是否持久化 | 说明 |
|----------|------------|------|
| **源代码** | ✅ 持久化 | 在宿主机目录，通过挂载同步 |
| **编译产物**（install/build/log） | ✅ 持久化 | 同上 |
| **apt 安装的包** | ❌ 会丢失 | 在容器内，Rebuild 后消失 |
| **pip 安装的包** | ❌ 会丢失 | 同上 |
| **环境变量** | ❌ 会丢失 | 容器重建后重置 |
| **~/.bashrc 修改** | ❌ 会丢失 | 容器重建后重置 |

### 4.4 验证容器状态

```bash
# 在宿主机执行，查看容器状态
docker ps -a | grep <容器名>
```

---

## 五、持久化方案

### 5.1 方案一：Docker Volume（推荐）

修改 `.devcontainer/devcontainer.json`：

```json
{
  "name": "ROS2 Humble Dev",
  "image": "osrf/ros:humble-desktop-full",
  "workspaceFolder": "/workspaces/ros2_ws",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/ros2_ws,type=bind",
  
  "mounts": [
    "source=ros2-apt-cache,target=/var/cache/apt,type=volume",
    "source=ros2-pip-cache,target=/root/.cache/pip,type=volume"
  ],
  
  "postCreateCommand": "sudo apt-get update"
}
```

### 5.2 方案二：自定义 Dockerfile

创建 `.devcontainer/Dockerfile`：

```dockerfile
FROM osrf/ros:humble-desktop-full

# 安装常用工具（会持久化到镜像层）
RUN apt-get update && apt-get install -y \
    gdb \
    vim \
    python3-pip \
    && rm -rf /var/lib/apt/lists/*

# 设置环境变量
ENV ROS_DOMAIN_ID=0
```

修改 `devcontainer.json`：

```json
{
  "name": "ROS2 Humble Dev",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "workspaceFolder": "/workspaces/ros2_ws"
}
```

### 5.3 方案三：初始化脚本

创建 `.devcontainer/post-create.sh`：

```bash
#!/bin/bash
set -e

# 安装依赖
sudo apt-get update
sudo apt-get install -y gdb vim

# 配置环境
echo "source /opt/ros/humble/setup.bash" >> ~/.bashrc
echo "export ROS_DOMAIN_ID=0" >> ~/.bashrc

# 安装 Python 包
pip3 install colcon-common-extensions
```

在 `devcontainer.json` 中引用：

```json
{
  "postCreateCommand": "bash .devcontainer/post-create.sh"
}
```

---

## 六、生命周期钩子

| 钩子 | 执行时机 | 用途 |
|------|----------|------|
| `postCreateCommand` | 容器**首次创建**时 | 安装依赖、初始化环境 |
| `postStartCommand` | 容器**每次启动**时 | 启动服务、检查状态 |
| `postAttachCommand` | VSCode **附加**到容器时 | 最终初始化 |

---

## 七、文件同步机制

```
宿主机修改文件 ──自动同步──► 容器内立即可见
容器内编译生成 ──自动同步──► 宿主机立即可见（install/build/log）
```

这就是为什么在容器内 `colcon build`，宿主机也能看到生成的文件。

---

## 八、总结

```
┌─────────────────────────────────────────────────────────┐
│  退出 VSCode → 容器停止，但保留                         │
│  再次打开   → 重启原容器，数据都在                      │
│  Rebuild    → 删除容器，重建新容器，容器内安装的东西丢失 │
│                                                       │
│  持久化关键：                                          │
│  • 源代码 → 已自动持久化（挂载宿主机目录）              │
│  • 依赖包 → 用 Dockerfile 或 Volume 持久化             │
└─────────────────────────────────────────────────────────┘
```

**建议**：把常用依赖写在 `Dockerfile` 或 `postCreateCommand` 中，这样 Rebuild 后能自动恢复开发环境。
